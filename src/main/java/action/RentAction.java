/*
package action;

import engine.GameEngine;
import model.player.Player;

public class RentAction implements ActionHandler {
    private static final int RENT_M = 3;

    @Override
    public void execute(Player attacker, Player defender, GameEngine game) {
        int paid = Math.min(RENT_M, defender.getMoneyM());
        defender.addMoney(-paid);
        attacker.addMoney(paid);
        System.out.println(attacker.getName() + " 使用「收租」：从 " + defender.getName() + " 处收到 " + paid + "M。");
    }
}

 */

